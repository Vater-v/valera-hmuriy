# Valera Hmuriy (Валера Хмурый) 🎲🤖

**Valera Hmuriy** — это продвинутый бот и помощник для мобильных нард (Backgammon). Проект объединяет Android-приложение с оверлеем, нативный модуль для перехвата функций игры (hooking) и мощный движок искусственного интеллекта GNU Backgammon для принятия решений.

## 📂 Структура проекта

Проект разделен на несколько ключевых компонентов:

* **`valera-app/`**: Android-приложение (Kotlin).
* Отвечает за пользовательский интерфейс (UI).
* Отрисовывает оверлей (Overlay) поверх игры.
* Управляет сервисом `ValeraService`.


* **`hmuriy_module/`**: Нативный модуль (C++).
* Использует `And64InlineHook` для внедрения в процесс игры.
* Читает память и состояние игры.
* Выполняет хуки игровых функций (`GameHooks.cpp`).


* **`gnubg/` & `gnubg_engine/**`: Движок GNU Backgammon.
* Портированная или адаптированная версия движка для анализа позиций.
* Содержит логику игры (`logic.py`), модели (`models.py`) и базы данных весов.


* **`hmuriy/`**: "Мозг" бота (Python).
* Серверная часть, принимающая решения (`decisions.py`).
* Взаимодействует с API (`api_client.py`).


* **`web/`**: Веб-интерфейс.
* **`gsservice/`**: Дополнительный сервис.

## 🚀 Функциональность

* **Автоматическая игра:** Бот может самостоятельно играть матчи, принимая оптимальные решения.
* **Подсказки (Hints):** Отображение лучших ходов через оверлей на экране Android.
* **Анализ GNU Backgammon:** Использование одного из сильнейших в мире движков для оценки вероятностей (Equity) и куба удвоения.
* **Memory Hooking:** Прямое чтение состояния доски из памяти процесса игры (вместо ненадежного распознавания изображений).
* **Веб-мониторинг:** Возможность следить за игрой удаленно.

## 🛠 Технологический стек

* **Android:** Kotlin, Android SDK, Gradle.
* **Native:** C++, CMake, And64InlineHook (для ARM64 хуков).
* **AI/Backend:** Python 3.13, GNU Backgammon (C/C++).
* **Web:** HTML, CSS, JavaScript.

## ⚙️ Сборка и установка

### Предварительные требования

* Android Studio
* Android NDK (для сборки C++ части)
* Python 3.x

### 1. Сборка модуля (hmuriy_module)

Модуль использует CMake.

```bash
cd hmuriy_module
# Используйте build.bat или команды cmake для компиляции под целевую архитектуру (обычно arm64-v8a)

```

### 2. Сборка приложения (valera-app)

Откройте папку `valera-app` в Android Studio и соберите APK.

* Убедитесь, что файл `local.properties` настроен корректно (путь к SDK/NDK).

### 3. Настройка Python окружения

Для работы скриптов анализа:

```bash
cd hmuriy
pip install -r requirements.txt

```

## ⚠️ Дисклеймер

Этот проект создан исключительно в образовательных и исследовательских целях (Reverse Engineering, AI).
Использование ботов в онлайн-играх может нарушать Условия использования (ToS) игровых платформ и привести к блокировке аккаунта. Автор не несет ответственности за любое использование данного ПО.

## 📜 Лицензия

Проект использует компоненты **GNU Backgammon**, который распространяется под лицензией GPL. Соответственно, производные части, использующие код gnubg, также могут подпадать под действие GPL.

---

*Разработано: vater-v / valera-hmuriy*
